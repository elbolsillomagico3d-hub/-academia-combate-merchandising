```html
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="Camisetas oficiales de la Academia de Combate">
  <title>Camisetas | Academia de Combate</title>

  <style>
    :root {
      --green: #173b00;
      --green-dark: #0d2500;
      --green-light: #2f650d;
      --cream: #f7f6f0;
      --white: #ffffff;
      --text: #20251d;
      --muted: #687062;
      --line: #dfe3d8;
      --shadow: 0 18px 45px rgba(20,40,10,.12);
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      font-family: Arial, Helvetica, sans-serif;
      background: var(--cream);
      color: var(--text);
      line-height: 1.5;
    }

    header {
      background: linear-gradient(135deg, var(--green-dark), var(--green));
      color: white;
      position: sticky;
      top: 0;
      z-index: 100;
      box-shadow: var(--shadow);
    }

    .header-inner {
      max-width: 1200px;
      margin: auto;
      padding: 14px 20px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 20px;
    }

    .brand {
      display: flex;
      align-items: center;
      gap: 12px;
      text-decoration: none;
      color: white;
    }

    .brand img {
      width: 55px;
      height: 55px;
      object-fit: contain;
    }

    .brand-text strong {
      display: block;
      font-size: 17px;
      letter-spacing: 1px;
    }

    .brand-text span {
      font-size: 13px;
      opacity: .8;
    }

    .cart-button {
      border: none;
      background: white;
      color: var(--green);
      padding: 11px 17px;
      border-radius: 999px;
      font-weight: bold;
      cursor: pointer;
      white-space: nowrap;
    }

    .cart-button:hover {
      background: #edf1e8;
    }

    nav {
      background: var(--green);
      border-top: 1px solid rgba(255,255,255,.12);
    }

    .nav-inner {
      max-width: 1200px;
      margin: auto;
      padding: 9px 20px;
      display: flex;
      gap: 8px;
      overflow-x: auto;
    }

    nav a {
      color: white;
      text-decoration: none;
      padding: 7px 12px;
      border-radius: 8px;
      font-size: 14px;
      white-space: nowrap;
    }

    nav a:hover,
    nav a.active {
      background: rgba(255,255,255,.15);
    }

    main {
      max-width: 1200px;
      margin: auto;
      padding: 45px 20px 70px;
    }

    .hero {
      text-align: center;
      margin-bottom: 40px;
    }

    .hero h1 {
      color: var(--green);
      font-size: clamp(35px, 6vw, 58px);
      margin-bottom: 10px;
    }

    .hero p {
      color: var(--muted);
      font-size: 18px;
      max-width: 700px;
      margin: auto;
    }

    .product-layout {
      display: grid;
      grid-template-columns: 1.1fr .9fr;
      gap: 35px;
      align-items: start;
    }

    .product-image-card {
      background: white;
      border: 1px solid var(--line);
      border-radius: 24px;
      padding: 25px;
      box-shadow: var(--shadow);
      position: sticky;
      top: 145px;
    }

    .main-image {
      width: 100%;
      height: 500px;
      object-fit: contain;
      cursor: zoom-in;
      border-radius: 18px;
      background: var(--cream);
    }

    .image-help {
      text-align: center;
      color: var(--muted);
      font-size: 13px;
      margin-top: 10px;
    }

    .product-card {
      background: white;
      border: 1px solid var(--line);
      border-radius: 24px;
      padding: 30px;
      box-shadow: var(--shadow);
    }

    .product-card h2 {
      color: var(--green);
      font-size: 30px;
      margin-bottom: 8px;
    }

    .description {
      color: var(--muted);
      margin-bottom: 22px;
    }

    .price-box {
      background: var(--cream);
      border-radius: 16px;
      padding: 18px;
      margin-bottom: 25px;
    }

    .price {
      font-size: 34px;
      font-weight: bold;
      color: var(--green);
    }

    .price-note {
      font-size: 13px;
      color: var(--muted);
    }

    .section {
      margin-top: 25px;
    }

    .section-title {
      display: block;
      font-weight: bold;
      margin-bottom: 10px;
    }

    .variants {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 10px;
    }

    .variant {
      border: 2px solid var(--line);
      border-radius: 12px;
      padding: 10px;
      background: white;
      cursor: pointer;
      text-align: center;
      transition: .2s;
    }

    .variant:hover {
      border-color: var(--green-light);
    }

    .variant.active {
      border-color: var(--green);
      background: #f1f5eb;
    }

    .variant img {
      width: 100%;
      height: 95px;
      object-fit: contain;
      margin-bottom: 5px;
    }

    .sizes {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
    }

    .size {
      border: 1px solid var(--line);
      background: white;
      padding: 10px 15px;
      border-radius: 9px;
      cursor: pointer;
      font-weight: bold;
    }

    .size.active {
      background: var(--green);
      color: white;
      border-color: var(--green);
    }

    .quantity {
      display: flex;
      align-items: center;
      gap: 12px;
    }

    .quantity input {
      width: 90px;
      padding: 12px;
      border: 1px solid var(--line);
      border-radius: 9px;
      font-size: 16px;
    }

    .add-button {
      width: 100%;
      margin-top: 25px;
      padding: 16px;
      border: none;
      border-radius: 12px;
      background: var(--green);
      color: white;
      font-size: 17px;
      font-weight: bold;
      cursor: pointer;
    }

    .add-button:hover {
      background: var(--green-light);
    }

    .shipping {
      margin-top: 40px;
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 15px;
    }

    .shipping-card {
      background: white;
      border: 1px solid var(--line);
      border-radius: 16px;
      padding: 20px;
      text-align: center;
    }

    .shipping-card strong {
      display: block;
      color: var(--green);
      font-size: 20px;
      margin-bottom: 5px;
    }

    .shipping-card span {
      color: var(--muted);
      font-size: 14px;
    }

    .cart-section,
    .order-section {
      margin-top: 45px;
      background: white;
      border: 1px solid var(--line);
      border-radius: 24px;
      padding: 30px;
      box-shadow: var(--shadow);
    }

    .cart-section h2,
    .order-section h2 {
      color: var(--green);
      margin-bottom: 20px;
    }

    #cartItems {
      display: grid;
      gap: 10px;
    }

    .cart-item {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 15px;
      padding: 15px;
      background: var(--cream);
      border-radius: 12px;
    }

    .cart-item-info strong {
      display: block;
    }

    .cart-item-info span {
      font-size: 13px;
      color: var(--muted);
    }

    .cart-item-price {
      font-weight: bold;
      color: var(--green);
      white-space: nowrap;
    }

    .remove-button {
      border: none;
      background: #eee;
      padding: 7px 10px;
      border-radius: 7px;
      cursor: pointer;
    }

    .cart-total {
      text-align: right;
      margin-top: 20px;
      font-size: 22px;
      font-weight: bold;
      color: var(--green);
    }

    .empty-cart {
      color: var(--muted);
      padding: 10px 0;
    }

    form {
      display: grid;
      gap: 15px;
    }

    .form-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 15px;
    }

    label {
      display: grid;
      gap: 6px;
      font-weight: bold;
      font-size: 14px;
    }

    input,
    textarea {
      width: 100%;
      padding: 12px;
      border: 1px solid var(--line);
      border-radius: 9px;
      font-family: inherit;
      font-size: 15px;
    }

    textarea {
      min-height: 100px;
      resize: vertical;
    }

    .payment {
      margin-top: 25px;
      padding: 20px;
      border-radius: 15px;
      background: var(--cream);
    }

    .payment h3 {
      color: var(--green);
      margin-bottom: 10px;
    }

    .payment p {
      margin: 5px 0;
    }

    .submit-button {
      padding: 15px;
      border: none;
      border-radius: 10px;
      background: var(--green);
      color: white;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
    }

    .success {
      display: none;
      margin-top: 20px;
      padding: 18px;
      border-radius: 12px;
      background: #e9f3df;
      color: var(--green);
      font-weight: bold;
    }

    footer {
      background: var(--green-dark);
      color: white;
      text-align: center;
      padding: 30px 20px;
    }

    /* MODAL DE ZOOM */

    .zoom-modal {
      display: none;
      position: fixed;
      inset: 0;
      background: rgba(0,0,0,.85);
      z-index: 500;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }

    .zoom-modal.show {
      display: flex;
    }

    .zoom-modal img {
      max-width: 95%;
      max-height: 90vh;
      object-fit: contain;
    }

    .close-zoom {
      position: absolute;
      top: 20px;
      right: 25px;
      color: white;
      font-size: 40px;
      cursor: pointer;
    }

    @media (max-width: 850px) {
      .product-layout {
        grid-template-columns: 1fr;
      }

      .product-image-card {
        position: static;
      }

      .main-image {
        height: 400px;
      }

      .shipping {
        grid-template-columns: 1fr;
      }
    }

    @media (max-width: 600px) {
      .header-inner {
        padding: 10px 14px;
      }

      .brand img {
        width: 45px;
        height: 45px;
      }

      .brand-text strong {
        font-size: 13px;
      }

      .brand-text span {
        font-size: 11px;
      }

      .cart-button {
        padding: 9px 12px;
        font-size: 13px;
      }

      main {
```
